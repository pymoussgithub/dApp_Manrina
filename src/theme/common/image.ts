import { StyleSheet } from 'react-native';

export const image = StyleSheet.create({
    thumbnail: {
        width: 50,
        height: 50,
        resizeMode: 'contain',
    },
    icon: {
        width: 24,
        height: 24,
        resizeMode: 'contain',
        justifyContent: 'center',
        alignItems: 'center',
    },
});
